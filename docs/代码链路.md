# Auto Slice 代码链路

## 2026-08-08 S01 插件骨架与契约装载

**触达**:
- `.codex-plugin/plugin.json` — 声明可由 Codex 装载的 `auto-slice` 本地插件。
- `src/controller/main.ts:runController` — 提供 `inspect-contracts` 进程入口及闭合错误输出。
- `src/contracts/load-frozen-contracts.ts:loadFrozenContracts` — 严格读取 UTF-8 冻结文档、校验 v1 清单并计算 SHA-256。
- `src/contracts/workspace-identity.ts:createWorkspaceIdentity` — 归一化真实路径并生成稳定工作区身份。
- `scripts/verify-s01.mjs:main` — 以 argv checks 生成 `CompletionReceipt` 与契约快照。

**入口**:`auto-slice-controller inspect-contracts [workspace_root]`，或 `node dist/src/controller/main.js inspect-contracts .`。
**测试**:`test/load-frozen-contracts.test.ts` 覆盖路径等价、digest 变化、只读装载和失败闭合；`test/controller.test.ts` 覆盖进程状态与退出码。

## 2026-08-08 S02 持久化 Run 状态机

**触达**:
- `src/controller/state/file-run-store.ts:FileRunStore` — 以不可变版本事件、原子 snapshot 和严格重放实现持久 RunStore 与 CAS。
- `src/controller/state/file-run-store.ts:createEffectIdempotencyKey` — 固定 `run_id/state_version/action/stable_target_id` 的幂等键与副作用意图/回执账本。
- `src/controller/state/transitions.ts:buildRunTransitionMatrix` — 枚举主状态机、压缩接力状态和失败闭合转换。
- `src/controller/state/schema.ts:ensureStateStoreSchema` — 初始化 v1 迁移并拒绝未知 schema version。
- `scripts/replay-run-events.mjs:main` — 只读重放事件链并输出确定性 RunReplayReport。
- `scripts/verify-s02.mjs:main` — 执行物化 checks，生成转换矩阵、崩溃报告与 CompletionReceipt。

**入口**:`FileRunStore.open(storage_root)` 后调用 `create/load/compareAndSwap`，或 `npm run state:replay -- <storage_root> <run_id>`。
**测试**:`test/run-store.test.ts` 覆盖全转换矩阵、跨进程 CAS、重复重放、事件 golden、snapshot 损坏、两个副作用崩溃断点和未知 schema；`scripts/verify-s02-replay.mjs` 断言重放工具零写入。

## 2026-08-08 S03 Project Write Lease 与 Protected Change

**触达**:
- `src/controller/workspace/file-workspace-guard.ts:FileWorkspaceGuard` — 以 active locator 原子硬链接串行化工作区写入，并用不可变事件实现续租、冻结、epoch 轮换和释放。
- `src/controller/workspace/file-workspace-guard.ts:FileWorkspaceGuard.assertWritable` — 拒绝冻结、过期、丢失或旧 epoch 的写 capability。
- `src/controller/workspace/git-change-guard.ts:GitChangeGuard.captureBaseline` — 从真实 Git HEAD、index、worktree 和 untracked 文件固化 Protected Change 基线。
- `src/controller/workspace/git-change-guard.ts:GitChangeGuard.classify` — 以整文件边界区分 protected、Slice-owned、重叠和未声明改动。
- `scripts/verify-s03-evidence.mjs:main` — 生成双进程竞争、Git 场景矩阵和租约事件链回执。
- `scripts/verify-s03.mjs:main` — 执行物化 checks 并生成 S03 CompletionReceipt。

**入口**:`FileWorkspaceGuard.open(storage_root)` 后调用 `acquire/renew/freezeWrites/rotateEpoch/release`；Git 路径从 `GitChangeGuard.captureBaseline(workspace)` 开始。
**测试**:`test/workspace-guard.test.ts` 覆盖跨进程唯一胜者、重启恢复、expiry/epoch fail-closed，以及 clean、untracked、staged、unstaged、rename、deleted、同文件重叠和 HEAD 漂移的真实 Git 仓库。

## 2026-08-08 S04 Deterministic Model Router

**触达**:
- `src/controller/model-policy/model-router.ts:MODEL_POLICY_TABLE` — 固定 DEVELOPMENT/CONTINUATION、COMPRESSION 与 DETERMINISTIC 的精确模型决策。
- `src/controller/model-policy/model-router.ts:ModelRouter.resolve` — 校验 Host 能力快照来源与有效期，并对缺模型、缺 effort、未知角色及失效快照返回 `model_policy_unavailable`。
- `src/controller/model-policy/types.ts:HostModelCapabilityAdapter` — 定义只提供带来源、采集时间和截止时间的 Host 能力快照端口。
- `scripts/verify-s04-evidence.mjs` — 生成四角色策略、失败闭合与 DETERMINISTIC 零 provider request 证据。
- `scripts/verify-s04.mjs:main` — 执行物化 checks 并生成 S04 CompletionReceipt。

**入口**:`new ModelRouter(clock).resolve(role, capabilities)`；后继 S05/S09/S10 只消费成功的 `ModelDecision`。
**测试**:`test/model-router.test.ts` 覆盖四角色全矩阵、精确模型/effort 缺失、未知角色、无效/未来/过期快照、零能力读取及失效时钟。

## 2026-08-08 S05 Slice 执行与确定性验收

**触达**:
- `src/controller/slices/contract-parser.ts:parseSliceContractV1` — 把 SliceSpec/运行时输入收敛为 argv-only、workspace-relative 的 `SliceContractV1`。
- `src/controller/slices/process-runner.ts:CheckProcessRunner.run` — 以环境 allowlist、输出上限和真实进程树终止执行确定性 check。
- `src/controller/slices/slice-executor.ts:SliceExecutor.start/collect` — 校验 DEVELOPMENT 决策与 Project Write Lease，固化执行前后 Git 快照并收集 check 回执。
- `src/controller/slices/verifier.ts:SliceVerifier.verify` — 复核执行回执、artifact digest 与 Slice-owned diff，按枚举错误 fail closed。
- `scripts/verify-s05-evidence.mjs:main` — 生成真实进程矩阵、artifact 失败矩阵和可重复验收回执样本。
- `scripts/verify-s05.mjs:main` — 消费 S02/S03/S04 回执，执行物化 checks 并生成 S05 CompletionReceipt。

**入口**:`SliceExecutor.start(contract, lease, modelDecision)` → `collect(executionId)` → `SliceVerifier.verify(contract, receipt, workspace)`。
**测试**:`test/slice-executor.test.ts` 覆盖契约越界、租约/模型/execution 生命周期、成功/非零/spawn/超时/超量输出、整棵子进程树终止、artifact 缺失/错 digest、LLM 通过自述失效、Protected Change、越界写、回执篡改和重复验收。

## 2026-08-08 S06 Commit Mode 与 Checkpoint

**触达**:
- `src/controller/git/commit-coordinator.ts:CommitCoordinator.finishSlice` — 复核 PASS VerificationReceipt、实时 OwnedPatch 与 HEAD，按 Run/Slice mode 编排 commit 或 diff-only 完成路径。
- `src/controller/git/commit-coordinator.ts:CommitCoordinator.prepareAlternateIndex` — 从干净临时 index 精确 stage OwnedPatch，校验 path/blob/patch 后提交，并保留调用者原有 index。
- `src/controller/git/checkpoint-writer.ts:CheckpointWriter.atomicRewrite` — 同目录临时文件写入、flush 后原子 rename `SESSION_CHECKPOINT.md`。
- `src/controller/git/git-process-runner.ts:GitProcessRunner.run` — 仅以 argv 调用本地 Git，不提供 push 或远端写入路径。
- `scripts/verify-s06-evidence.mjs:main` — 生成提交树、归一化 checkpoint 与 hook/HEAD/stage/rename 失败现场证据。
- `scripts/verify-s06.mjs:main` — 消费 S03/S05 回执，执行物化 checks 并生成 S06 CompletionReceipt。

**入口**:`CommitCoordinator.finishSlice({ run, slice, verification, protected_baseline, owned_patch, checkpoint })`。
**测试**:`test/commit-coordinator.test.ts` 覆盖 after_slice 精确提交、none/override、Protected Change 保留、失败/篡改验收、hook 失败、HEAD 竞争、stage scope、checkpoint rename 失败和非 Git worktree。

## 2026-08-09 S07 自动压缩 30 秒检测

**触达**:
- `src/controller/compaction-monitor/compaction-monitor.ts:CompactionMonitor.onEvent` — 校验 Host 结构化 STARTED/COMPLETED、线程身份和有序序号，并以 RunStore CAS 持久化等待或恢复转换。
- `src/controller/compaction-monitor/compaction-monitor.ts:CompactionMonitor.onDeadline` — 以持久 deadline 裁决超时，CAS 唯一推进至 `SOURCE_INTERRUPTING`，并把竞争失败方闭合为诊断 no-op。
- `src/controller/compaction-monitor/compaction-monitor.ts:CompactionMonitor.recover` — Controller 重启时重挂未到期 timer，或对已逾期 compaction 立即提交 deadline。
- `src/controller/compaction-monitor/timeout-scheduler.ts:TimeoutDeadlineScheduler` — 提供生产 Clock/Scheduler 适配器；普通静默不会自行创建 timer。
- `scripts/verify-s07-evidence.mjs:buildVirtualClockTrace/buildRaceReport` — 生成时间边界、重启、60 秒静默和双进程 CAS 重复竞态证据。
- `scripts/verify-s07.mjs:main` — 消费 S02 回执，执行物化 checks 并生成 S07 CompletionReceipt。

**入口**:`CompactionMonitor.onEvent(run_id, host_event, expected_state_version)`；重启入口为 `recover`，timer 回调入口为 `onDeadline`。
**测试**:`test/compaction-monitor.test.ts` 覆盖 29.999/30/30.001 秒、重复/乱序/未知事件、旧线程、可观测性缺失、重启前后和 CAS 竞态；`test/helpers/compaction-race-worker.ts` 提供真实双进程竞争。

## 2026-08-09 S08 Source Interruption 与写隔离

**触达**:
- `src/controller/thread-control/source-interruption-coordinator.ts:SourceInterruptionCoordinator.interruptSource` — 编排写冻结、effect intent、幂等中断、独立线程检查、receipt digest、epoch 轮换与 RunStore CAS。
- `src/controller/thread-control/source-interruption-coordinator.ts:decodeInterruptReceipt/decodeThreadInspection` — 严格拒绝停止未确认、身份/revision 漂移及已删除、归档或不可读的伪回执。
- `src/controller/thread-control/source-interruption-coordinator.ts:freezeOrRecover/rotateOrRecover` — 从不可变 lease 事件恢复冻结或已轮换位置，封锁崩溃重放造成的重复副作用。
- `src/controller/thread-control/types.ts:ThreadControlPort/InterruptReceipt/ThreadInspection` — 固化 Host 中断与持久线程独立核验端口。
- `scripts/verify-s08-evidence.mjs:buildThreadAdapterContractReport/buildLateWriteReport/buildFailureClosureMatrix` — 生成幂等中断、冻结期迟到写和失败闭合证据。
- `scripts/verify-s08.mjs:main` — 消费 S03/S07 回执，执行物化 checks 并生成 S08 CompletionReceipt。

**入口**:`SourceInterruptionCoordinator.interruptSource(run_id, lease_id, expected_write_epoch, expected_state_version)`。
**测试**:`test/thread-control.test.ts` 覆盖成功/重复/并发、冻结期与旧 epoch 写拒绝、超时、伪回执、删除/归档/不可读、revision 漂移，以及 epoch 轮换后 Run 转换前崩溃恢复。

## 2026-08-09 S09 Compression Task 与 Handoff

**触达**:
- `src/controller/handoff/compression-handoff-coordinator.ts:CompressionHandoffCoordinator.exportHandoff` — 以同态 CAS 单次认领 `compaction_id`，编排任务创建、技能回执、effect 账本与 `CONTINUATION_STARTING` 转换。
- `src/controller/handoff/compression-handoff-coordinator.ts:decodeLaunchReceipt/decodeAndVerifyHandoffReceipt` — 严格核验任务隔离、`gpt-5.6-sol/medium`、Handoff v2、Source revision、发布路径、文件 digest 与 synthesize-first consumer contract。
- `src/controller/state/file-run-store.ts:assertMetadataTransition` — 只允许 `mark_handoff_attempted` 把 `handoff_attempted=false` 原子改为 `true`，重放时复核同一约束。
- `scripts/verify-s09-evidence.mjs:main/runMapWorker` — 使用 `export-codex-handoff` helper 对固定 rollout 执行隔离 MAP、REDUCE preflight、原子发布和真实 `verify-evidence`。
- `scripts/verify-s09.mjs:main` — 消费 S02/S04/S08 回执，执行物化 checks 并生成 S09 CompletionReceipt。

**入口**:`CompressionHandoffCoordinator.exportHandoff(run_id, interrupt_receipt, model_decision, expected_state_version)`。
**测试**:`test/handoff.test.ts` 覆盖成功/重复/并发、认领与终态崩溃恢复、任务隔离、worker/budget 失败、v1/revision/digest/缺件/verify-evidence 拒绝；`test/run-store.test.ts` 锁定受限同态 CAS。

## 2026-08-09 S10 Continuation Task 接力

**触达**:
- `src/controller/continuation/continuation-coordinator.ts:ContinuationCoordinator.continueFromHandoff` — 编排第三个全新只读任务、Handoff 双重复验、Ready/Lease/Progress 回执和最终原子 Source 替换。
- `src/controller/continuation/continuation-coordinator.ts:decodeReadyReceipt` — 按 synthesize-first consumer contract 拒绝 pre-draft 读取、超额定向读取、broad search、full-file reread 与 workspace 漂移。
- `src/controller/continuation/continuation-coordinator.ts:decodeProgressReceipt` — 仅接受变化后的持久产物 digest 或 VerificationReceipt digest，普通 commentary 不构成进展。
- `src/controller/continuation/continuation-coordinator.ts:closeFailure` — grant 可能已发生时重新冻结当前 epoch，并以 `NEEDS_USER` 保留 Handoff、旧 Source 和诊断。
- `scripts/verify-s10-evidence.mjs:main` — 从真实 RunStore、Lease 事件与持久 rollout 生成三任务轨迹、租约日志、进展回执和失败闭合矩阵。
- `scripts/verify-s10.mjs:main` — 绑定 S03/S04/S09 前驱回执，执行物化 checks 并生成 S10 CompletionReceipt。

**入口**:`ContinuationCoordinator.continueFromHandoff({ run_id, lease_id, handoff_receipt, slice_contract, model_decision, expected_owned_diff_digest, expected_state_version })`。
**测试**:`test/continuation.test.ts` 覆盖成功/重放/并发、三 UUID 隔离、Ready 前禁写、Handoff 替换、workspace/consumer-contract 拒绝、grant 模糊失败冻结、持久进展门禁、模型失败与 stale caller。

## 2026-08-09 S10 验收证据可重复核验

**触达**:
- `scripts/verify-s10.mjs:normalizeContinuationTraceForComparison` — 先复算并验证每次真实 rollout digest，再仅规范化由随机临时 Handoff 绝对路径派生的两个跨运行字段。
- `scripts/verify-s10.mjs:assertEvidenceMatches` — 对 continuation trace 使用上述语义规范化，其余 S10 evidence 仍保持严格结构比较。

**入口**:`npm run verify:s10` 对已生成 S10 evidence 与新鲜独立运行结果做只读复核。
**测试**:两次 `scripts/verify-s10-evidence.mjs` 复现仅路径派生 digest 变化；`npm run verify:s10 -- --regenerate` 后以未改代码的 `npm run verify:s10` 锁定回归。

## 2026-08-09 S11 控制面核心

**触达**:
- `src/controller/control-plane/control-plane.ts:ControlPlane.execute` — 以持久 `command_id` 回执编排 start/status/pause/resume/abort/override，先校验 version 与恢复决议再调用副作用端口。
- `src/controller/control-plane/file-command-journal.ts:FileCommandJournal` — 原子发布命令 intent/completion，跨进程重启返回原回执并拒绝同 ID 异构重放。
- `src/controller/control-plane/status-projector.ts:projectRunSnapshot` — 投影任务 UUID、最近成功状态、错误码与证据路径，同时过滤原始错误输出。
- `src/controller/control-plane/recovery-catalog.ts:RECOVERY_CATALOG` — 为既有 `NEEDS_USER` 错误固定匹配恢复或仅 abort 的闭合方式。
- `src/controller/state/file-run-store.ts:assertMetadataTransition` — 允许且严格限制单个 Slice commit-mode override 的同状态 CAS。

**入口**:`new ControlPlane(ports).execute(command, envelope)`；所有修改命令必须携带 `expected_state_version`。
**测试**:`test/control-plane.test.ts` 覆盖重放/重启、pause-resume、pause-abort 竞争、override 门禁、显式恢复、脱敏 status 与恢复目录全覆盖；`test/run-store.test.ts` 锁定旧事件兼容。

## 2026-08-09 S11 插件命令与文件运行时

**触达**:
- `src/controller/main.ts:runController` — 暴露 start/status/pause/resume/abort/override 的 JSON envelope CLI，并保留只读 `inspect-contracts`。
- `src/controller/control-plane/runtime.ts:openFileControlPlane` — 组合真实 RunStore、WorkspaceGuard 与 CommandJournal；pause 冻结 lease、resume 轮换 epoch、abort 释放写能力。
- `src/controller/control-plane/runtime.ts:EvidenceBoundRecovery` — 只接受 workspace 内真实文件及匹配 digest 作为非 abort 恢复证据。
- `skills/auto-slice-control/SKILL.md` — 规定先 status、携带 expected version、由用户显式选择恢复且绝不 push 的插件操作流程。
- `.codex-plugin/plugin.json` — 声明 `./skills/` 并展示控制面 starter prompts。

**入口**:`auto-slice-controller <command> <request_json_path> [storage_root]`，插件 skill 将用户操作翻译为同名命令。
**测试**:`test/controller.test.ts` 覆盖真实文件 start/status；官方 plugin validator 校验 manifest 与 skill 结构。

## 2026-08-09 S11 验收契约与证据

**触达**:
- `contracts/slices/S11.json` — 冻结 S02/S05/S06/S10 输入、S11 owned paths、六项 checks 与完成产物。
- `src/controller/control-plane/dto-schema.ts:CONTROL_COMMAND_DTO_SCHEMA` — 发布六命令、乐观版本与显式恢复证据的 JSON Schema。
- `scripts/verify-s11-evidence.mjs:buildMatrixEvidence/buildRecoveryEvidence/buildInvariantReport` — 生成 70-cell 控制矩阵、错误目录覆盖和无 push/无模型 fallback/脱敏状态证明。
- `scripts/verify-s11.mjs:main` — 执行 build/typecheck/test/evidence/plugin validation/lint，生成并复核 S11 CompletionReceipt。

**入口**:`npm run verify:s11 -- --regenerate` 生成新鲜证据；`npm run verify:s11` 只读复核现有 S11 回执。
**测试**:S11 verifier 绑定全量 test runner、官方插件 validator、证据结构比对、owned-path 边界与输出 digest。

## 2026-08-09 S12 真实验收夹具与文档收口

**触达**:
- `scripts/run-s12-e2e.mjs:main` — 在系统临时目录串行运行十个冻结场景，组合真实 Git/process/文件锁、虚拟时钟和持久任务适配器。
- `scripts/run-s12-e2e.mjs:PersistentTaskHost` — 以磁盘任务记录驱动 Source interruption、Compression Handoff 与 Continuation 接力，并暴露可计数副作用。
- `scripts/verify-s12.mjs:main` — 连跑两次 harness、复核 S01–S11 回执、生成 S12 coverage/checklist/场景证据与 CompletionReceipt。
- `scripts/check-markdown-links.mjs` — 对 README 与 docs 的相对 Markdown 链接执行存在性检查。
- `test/fixtures/s12/scenarios.json` — 冻结十场景顺序、结构化 Host 事件、三任务 UUID 与时间边界。
- `contracts/slices/S12.json` — 声明 S12 输入摘要、owned paths、八项确定性 checks 和发布前产物。

**入口**:`npm run verify:s12 -- --regenerate` 生成新鲜证据；`npm run verify:s12` 独立重跑并只读复核现有 S12 回执。
**测试**:harness 覆盖 after_slice/none、Protected Change、29.999/30 秒压缩边界、三类失败闭合、崩溃恢复和双 Run 竞争；verifier 额外执行 build/typecheck/138+ tests/lint/plugin/Markdown 链接与全部 Slice contract coverage。

## 2026-08-09 S13 Production Plan 边界

**触达**:
- `src/controller/production/plan-parser.ts:parseProductionPlanV1` — 严格归一化有序生产计划，拒绝越界路径、重复 Slice、未知字段和不可满足的精确模型能力。
- `src/controller/production/prompt-builder.ts:buildDevelopmentPrompt` — 只生成一句 `设定goal`；Slice 契约、instructions 和 controller invariants 不进入任务 input。
- `src/controller/production/types.ts:DevelopmentTaskPort` — 定义可注入的开发任务启动、结构化 compaction 事件流和完成回执端口。
- `CONTEXT.md` — 固定 Production Plan、Development Task 与 Production Orchestrator 的领域含义。

**入口**:`parseProductionPlanV1(raw, clock)`；本切片只建立生产输入和端口，不启动 Codex 或推进 Run。
**测试**:`test/production-plan.test.ts` 覆盖合法计划规范化、路径逃逸、重复 Slice、模型/effort/时效失败、未知字段，以及 development/compression/continuation 三类精确短输入。

## 2026-08-09 S13 App Server Development Task 适配器

**触达**:
- `src/controller/production/app-server-client.ts:CodexAppServerClient` — 以有界 stdio JSONL 实现 App Server 初始化、请求、通知、超时和子进程失败闭合。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask` — 启动持久 DEVELOPMENT Source Thread，规范化 `contextCompaction` 生命周期，处理中断并独立重读持久线程。
- `test/fixtures/process/fake-codex-app-server.mjs` — 固定官方初始化、`workspace-write`、最小 thread/turn 响应与结构化事件契约，不启动真实 Codex。

**入口**:`DevelopmentTaskPort.start(request)`；超时接力复用同一实例的 `ThreadControlPort.interrupt/inspect`。
**测试**:`test/app-server-development-task.test.ts` 覆盖完成、中断、模型 reroute、畸形压缩生命周期和子进程退出。

## 2026-08-09 S13 Slice 级受保护基线

**触达**:
- `src/controller/state/file-run-store.ts:assertMetadataTransition` — 仅允许 Slice 身份与 `protected_baseline_digest` 成对地在 `PREPARING/CHECKPOINTING -> SLICE_RUNNING` 轮换。
- `test/run-store.test.ts:rotates the protected baseline only when starting the next Slice` — 锁定允许轮换、缺少 Slice 身份和其他状态改写的拒绝路径。

**入口**:`FileRunStore.compareAndSwap(run_id, version, transition)`；Production Orchestrator 在启动下一 Slice 时提交成对更新。
**测试**:`node --test dist/test/run-store.test.js` 覆盖 18 条 RunStore 状态、重放、CAS 与崩溃恢复用例。

## 2026-08-09 S13 Production Orchestrator

**触达**:
- `src/controller/production/production-orchestrator.ts:ProductionOrchestrator.run` — 顺序驱动一句话 Goal Task、确定性执行/验证、任务侧 commit/checkpoint 完成观察、下一 Slice 基线和 Run 完成。
- `src/controller/production/production-orchestrator.ts:ProductionOrchestrator.recoverTimedOutCompaction` — 依据持久超时状态依次调用 Source interruption、Handoff export 与 Continuation，并返回非终态接力回执。
- `src/controller/production/types.ts:ProductionOrchestratorOptions` — 固定 RunStore、租约、开发、Goal completion 观察、验证、压缩与三任务接力的可注入端口。
- `test/production-orchestrator.test.ts` — 用 fake ports 验证双 Slice 顺序、Slice 级 baseline 轮换、完整状态版本和超时三任务身份隔离。

**入口**:`ProductionOrchestrator.run(resolvedProductionPlan)`；调用方先通过 ControlPlane 建立匹配的 `PREPARING` Run。
**测试**:`node --test dist/test/production-orchestrator.test.js` 覆盖 `DONE` 与 `CONTINUATION_STARTED` 两种确定性结果，全程不启动真实 Codex 或模型。

## 2026-08-09 S13 生产文件运行入口

**触达**:
- `src/controller/production/file-production-runtime.ts:runProductionPlanFile` — 组合真实文件状态、工作区租约、任务侧完成 Git 观察、压缩监控与 Production Orchestrator，并续租长任务写租约。
- `src/controller/production/production-cli.ts:runProductionPlanCommand` — 把计划路径、工作区和外部状态目录封装为稳定 `run-plan` 进程入口及结构化结果。
- `src/controller/production/codex-app-server-task-host.ts:CodexAppServerTaskHost` — 复用 App Server Development Task/ThreadControl；未注入接力 launcher 时确定性失败闭合。
- `src/controller/main.ts:runControllerCli` — 异步路由 `run-plan`，保留既有同步控制命令行为。

**入口**:`auto-slice-controller run-plan <plan_json_path> <workspace_root> [storage_root]`。
**测试**:`test/file-production-runtime.test.ts` 覆盖真实 Git、外部状态、`commit_mode=none`、CLI composition 与接力端口失败闭合；`test/controller.test.ts` 覆盖参数门禁不构造 Task Host。

## 2026-08-09 S13 确定性发布验收

**触达**:
- `contracts/slices/S13.json` — 冻结 Production Plan 文件入口的输入、所有权、八项 checks 与安装前门禁。
- `scripts/run-s13-production-entry.mjs:main` — 两次创建无 remote 的临时 Git 项目，以 `commit_mode=none` 驱动真实文件入口并规范化断言。
- `test/fixtures/process/run-production-cli-with-fake-host.mjs` — 在独立进程组合默认 App Server Task Host 与确定性 Host 协议。
- `scripts/verify-s13.mjs:main` — 执行 build/typecheck/155 tests/双 harness/链接/插件/lint，生成并复核 S13 CompletionReceipt。

**入口**:`npm run verify:s13 -- --regenerate` 生成新鲜证据；`npm run verify:s13` 独立重跑并只读复核。
**测试**:`artifacts/s13/production-entry-report.json` 锁定 DONE、无 commit/head drift、remote/push=0 与工作区外状态目录；release checklist 保持生产工作区 Run 未授权。

## 2026-08-10 本地插件 CLI 路径别名兼容

**触达**:
- `src/controller/main.ts:isControllerEntryPath` — 对 CLI 入口路径与模块路径先解析真实路径，使 npm/NVM 的 junction 或 symlink 不再绕过主入口。
- `test/controller.test.ts:Controller recognizes an entry path reached through a directory link` — 用临时目录联接锁定路径别名与真实入口等价，且不同文件仍不匹配。

**入口**:`auto-slice-controller --help`；个人插件 Skill 通过全局 CLI 调用本地控制面。
**测试**:`node --test dist/test/controller.test.js` 覆盖目录联接入口识别；S13 verifier 继续覆盖生产入口与 `commit_mode=none` 隔离 harness。

## 2026-08-10 npm Codex App Server 启动修复

**触达**:
- `src/controller/production/app-server-client.ts:resolveNpmCodexCli` — 按 PATH 优先级或当前模块图解析 npm `@openai/codex/bin/codex.js`，由当前 Node 直接启动，避免 Windows 再解析到桌面版 `codex.exe`。
- `src/controller/production/app-server-client.ts:CodexAppServerClient.startAndInitialize` — 把同步与异步子进程启动异常统一闭合为 `app_server_spawn_failed`，不再伪装成协议错误。
- `test/app-server-client.test.ts` — 以隔离 npm 目录锁定默认启动来源，并覆盖同步 spawn 异常分类。

**入口**:`auto-slice-controller run-plan <plan_json_path> <workspace_root> [storage_root]` 的默认 `CodexAppServerTaskHost`。
**测试**:`node --test dist/test/app-server-client.test.js dist/test/app-server-development-task.test.js`、`npm test`（158 tests）与真实 npm `@openai/codex@0.146.0` 仅初始化探针通过。

## 2026-08-10 Codex App Server SandboxMode 协议修复

**触达**:
- `src/controller/production/codex-app-server-development-task.ts:CODEX_WORKSPACE_WRITE_SANDBOX` — 将 `thread/start` 的 workspace-write 权限序列化为 Codex v2 `SandboxMode` 要求的 kebab-case 枚举。
- `test/fixtures/process/fake-codex-app-server.mjs:thread/start handler` — 只接受 `workspace-write`，使错误的 camelCase wire value 在 Development Task 集成测试中失败。

**入口**:`CodexAppServerDevelopmentTask.start(request)` 创建持久 Source Thread 时发送 `thread/start`。
**测试**:修复前目标测试 1/4 通过且 3 项在 `thread/start` 失败；修复后目标测试 4/4、`npm run typecheck`、`npm run lint`、`npm test`（158/158）及 npm Codex CLI 0.146.0 生成 schema 对照全部通过。

## 2026-08-10 一句话 Goal Task 协议

**触达**:
- `src/controller/production/prompt-builder.ts:buildDevelopmentPrompt/buildCompressionPrompt/buildContinuationPrompt` — 固定三种短输入，禁止 Production Plan 与 controller invariants 泄漏进首条消息。
- `src/controller/production/goal-completion-guard.ts:GitGoalCompletionGuard.observe` — `none` 证明 HEAD 不变；`after_slice` 证明唯一非 merge、Slice-owned commit；两者都要求任务随后刷新 checkpoint。
- `src/controller/production/production-orchestrator.ts:ProductionOrchestrator.runSlice` — 把 Run/Slice commit mode 映射进 goal 文案，绑定完成观察与确定性 checks，不再调用 Controller commit/checkpoint 写路径。
- `src/controller/handoff/compression-handoff-coordinator.ts:createRequest` 与 `src/controller/continuation/continuation-coordinator.ts:createEnvelope` — 分别携带 `$export-codex-handoff <UUID>` 与 Handoff continuation goal。

**入口**:新 Slice、Compression Task 与 Continuation Task 的首个 `turn/start` input。
**测试**:`test/production-plan.test.ts` 锁定精确句子与 plan-data 隔离；`test/production-goal-completion.test.ts` 覆盖 none、正确 after_slice 和 checkpoint 误入 commit；相关 Production/Handoff/Continuation/File Runtime 集成测试覆盖实际透传。

## 2026-08-10 真实 App Server Goal 边界与插件交付

**触达**:
- `src/controller/production/production-cli.ts:runProductionPlanCommand` — 在无 remote 的临时 Git 工作区通过默认真实 App Server 完成 `commit_mode=none` Run，结果为 `DONE` 且 HEAD、提交数不变。
- `src/controller/production/prompt-builder.ts:buildDevelopmentPrompt` — Source Thread `019fea32-f1b2-7de2-93c6-d4b70cc00708` 的持久化 `user_message` 逐字等于一句 goal，plan objective、exclusion、instructions canary 均未进入任务输入。
- `skills/auto-slice-control/SKILL.md:Commit option and task input` — 发布到 `personal` marketplace，并以 cachebuster `0.1.0+codex.20260810054837` 重装；发布源与安装缓存的 Skill 哈希一致。

**入口**:`auto-slice-controller run-plan <plan_json_path> <workspace_root> <storage_root>` 与新任务中的 `auto-slice-control` Skill。
**测试**:真实 Run 的确定性 `check.mjs` 通过、checkpoint 刷新为 PASS、工作区仅留下 Slice-owned `result.txt` 与 checkpoint；`codex plugin list` 显示新版本 `installed, enabled`。

## 2026-08-10 内容盲控制面决策与切片方案

**触达**:
- `CONTEXT.md:Worker Content/Content-blind Controller` — 分开任务内容面与 Controller 可消费的有界元数据。
- `docs/adr/0009-content-blind-controller.md:§1` — 冻结零 Worker Content 控制边界、full-turn read 否决项与 revision 失败闭合。
- `docs/implementation-slices.md:S14–S16` — 拆分 App Server 事件防火墙、元数据 Source revision 和内容预算 E2E 门禁。
- `docs/auto-slice-design.md:§6.1` — 把内容盲不变量、Compression Task 唯一自动正文读取例外及决策索引纳入产品契约。

**入口**:实现从 S14 `SliceSpec` 开始，必须以 ADR-0009 和 S13 `CompletionReceipt` 为冻结输入。
**测试**:`node scripts/check-markdown-links.mjs` 校验新 ADR、S13 契约和决策索引链接；实现验收条件已在 S14–S16 中定义为确定性 checks。

## 2026-08-10 S14 App Server 事件防火墙

**触达**:
- `src/controller/production/app-server-client.ts:HostEventFirewall.project` — 把 App Server notification 重建为四类白名单 `ControllerSignal`，其余 Worker Content 返回 `DROP`。
- `src/controller/production/app-server-client.ts:CodexAppServerClient.startAndInitialize` — 通过 `optOutNotificationMethods` 精确关闭 message、reasoning、command、diff 与 plan 内容通知。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask.handleSignal` — 只消费已投影 DTO，保留 compaction/reroute/turn terminal 语义且不缓存原始 notification。
- `src/controller/production/types.ts:DevelopmentTaskReceipt` — 移除 `final_response_digest`，终态回执只保留身份、枚举结果与时间元数据。
- `scripts/run-s14-firewall.mjs:main` — 对短/大 Worker Content、六类 canary、四类控制信号和隔离 Production Run 生成可重复内容等价证明。
- `test/fixtures/process/run-s14-production-with-fake-host.mjs` — 把短/大正文场景接入真实文件运行时，以便比较规范化 Run 事件并扫描外部状态目录。
- `scripts/verify-s14.mjs:main` — 执行九项确定性 checks 并生成 whitelist、canary、content-equivalence 与 CompletionReceipt。

**入口**:`CodexAppServerClient.consumeLine` 解码 notification 后立即调用防火墙；Production Orchestrator 仍从 `DevelopmentTaskPort` 消费事件/回执。
**测试**:`test/app-server-client.test.ts` 锁定 opt-out 与 DTO 字段；`test/app-server-development-task.test.ts` 证明正文大小无关和 canary 零泄漏；既有 Production 目标测试锁定状态转换兼容。

## 2026-08-10 S15 Host 元数据 revision

**触达**:
- `src/controller/thread-control/types.ts:ThreadRevisionProvider/ThreadMetadataPort` — 固定 64-byte 不透明 token、`UNAVAILABLE` 与 summary-only inspection 端口。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerThreadMetadataPort` — 只发送 `thread/read(includeTurns=false)`，重建有界 summary 并拒绝任意 `turns/items`。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask.readPersistedRevision` — 使用可注入 Host revision provider，区分能力不可用与非法 token。
- `src/controller/production/codex-app-server-task-host.ts:CodexAppServerTaskHost` — 当前 Host 显式注入 fail-closed revision provider。
- `src/controller/thread-control/source-interruption-coordinator.ts:decodeInterruptReceipt/decodeThreadInspection` — 独立校验两个 opaque token，并把 unavailable/invalid/mismatch 保留为确定性失败原因。
- `test/fixtures/process/fake-codex-app-server.mjs:thread/read handler` — 拒绝 `includeTurns:true`，并提供恶意 `turns/items` summary 场景。
- `scripts/run-s15-metadata-revision.mjs:main` — 用真实 adapter、RunStore、lease 与 coordinator 生成六场景 capability、协议与 Handoff 门禁报告。
- `scripts/verify-s15.mjs:main` — 执行九项确定性 checks、双 harness 一致性，并生成 S15 CompletionReceipt。

**入口**:`ThreadControl.interrupt/inspect(source_thread_id)`；只有注入稳定 Host revision capability 才可形成 S08 revision 回执。
**测试**:`test/app-server-development-task.test.ts` 覆盖正常 token、默认 unavailable、非法 token、summary-only wire 请求与恶意正文夹带拒绝；`test/thread-control.test.ts` 覆盖 unavailable/invalid/mismatch 的 `NEEDS_USER` 闭合、冻结 lease 与零后续 inspection；`test/handoff.test.ts` 继续锁定 Source revision 漂移拒绝。

## 2026-08-10 S16 内容预算端到端门禁

**触达**:
- `contracts/slices/S16.json` — 冻结 S13–S15 回执、ADR-0009、8 KiB 信号预算、五场景矩阵与九项确定性 checks。
- `scripts/run-s16-content-budget.mjs:main` — 驱动普通 short/large、29.999 秒完成、30 秒 revision 可用/不可用五个隔离 Production 场景，比较 snapshot/effect/receipt/event 字节并扫描所有 Controller 表面。
- `test/fixtures/process/fake-s16-app-server.mjs:afterTurnStarted` — 在 fake Host 原始帧注入八类短/96 KiB canary，输出结构化 compaction/terminal 元数据且拒绝 full-turn read。
- `test/fixtures/process/run-s16-production-with-fake-host.mjs:DeterministicHandoffLaunchers` — 为 timeout 场景组合真实 App Server Task Host、opaque revision、Handoff v2 与 Continuation 三任务接力回执。
- `scripts/verify-s16.mjs:main` — 双跑 harness 并要求规范化 JSON 字节一致，生成 budget、canary、revision failure trace 与 CompletionReceipt。

**入口**:`npm run verify:s16 -- --regenerate` 生成新鲜证据；`npm run verify:s16` 只读重跑并复核现有回执。
**测试**:目标测试锁定 App Server firewall、metadata revision、Source interruption 与 Production Orchestrator；S16 harness 额外证明普通正文大小等价、29.999/30 秒边界、revision fail-closed、零 full-turn read、八类 canary 零命中和所有 ControllerSignal 不超过 8 KiB。

## 2026-08-10 S-TRUST-1 可信完成状态与兼容解码

**触达**:
- `src/controller/state/transitions.ts:isRunTransitionAllowed/isRunTransitionReplayCompatible` — 新 Run 只走 `SLICE_RUNNING -> PREPARING|DONE`，历史验收事件经独立只读谓词重放。
- `src/controller/state/file-run-store.ts:assertMetadataTransition/replayOrThrow` — 拒绝投递后修改当前 Slice override，同时保留旧事件链与既有 override 事件兼容。
- `src/controller/control-plane/control-plane.ts:ControlPlane.pause/resume/override` — 旧三态仅允许 status/abort，override 仅接受 `PENDING` Slice。
- `src/controller/control-plane/runtime.ts:CurrentSlicePhase` — 区分未投递、运行中、旧验收态和已完成 Slice 投影。
- `src/controller/production/production-orchestrator.ts:ProductionOrchestrator.runSlice` — 保留既有验收端口调用，但状态编排不再进入三旧态。

**入口**:`FileRunStore.compareAndSwap`、控制面 `status/pause/resume/abort/override` 与 Production Orchestrator 的结构化 Turn 完成路径。
**测试**:`test/run-store.test.ts` 覆盖直达状态矩阵及旧事件 replay；`test/control-plane.test.ts` 覆盖旧 snapshot status/abort 和投递后 override；Production/File Runtime 回归锁定端口调用保持且状态链无旧三态。

## 2026-08-10 S-TRUST-2 普通完成路径去验收

**触达**:
- `src/controller/production/production-orchestrator.ts:ProductionOrchestrator.runSlice` — 精确核对结构化 `COMPLETED` 身份与 digest 后直接推进并生成最小 Slice receipt，不再调用普通路径验收端口。
- `src/controller/production/production-orchestrator.ts:productionSliceBindingDigest/observeDevelopmentEvents` — 用 plan/slice 元数据维持状态绑定，仅在真实 compaction STARTED 事件后为既有超时接力惰性捕获 baseline。
- `src/controller/production/types.ts:ProductionOrchestratorOptions/ProductionSliceReceipt` — 把旧 executor/verifier/goal-completion 端口降为兼容注入，收敛回执为四个控制字段。
- `src/controller/production/file-production-runtime.ts:executeProductionPlanFile` — 普通 composition 不再捕获 Git baseline，也不构造 SliceExecutor、SliceVerifier 或 GitGoalCompletionGuard。
- `scripts/run-s14-firewall.mjs:normalizeProductionDecision`、`scripts/run-s16-content-budget.mjs:normalizeCliDecision` — 跟随最小 receipt 使用 `state_version`。
- `test/production-orchestrator.test.ts`、`test/file-production-runtime.test.ts` — 锁定四类旧端口零调用、非 Git/零产物完成、多 commit、无 checkpoint 与任意额外文件均不阻断。

**入口**:`ProductionOrchestrator.run(resolvedProductionPlan)` 收到匹配 Run/Slice/Thread/Turn 的 Development Task `COMPLETED` receipt。
**测试**:`node --test dist/test/production-orchestrator.test.js dist/test/file-production-runtime.test.js` 为 8/8；`npm test` 为 176/176，并通过 typecheck 与 lint。

## 2026-08-10 S-TRUST-3 压缩接力去工作区绑定

**触达**:
- `src/controller/production/production-orchestrator.ts:observeDevelopmentEvents/recoverTimedOutCompaction` — 压缩事件与 timeout 恢复不再捕获 baseline、workspace snapshot、change classification 或 OwnedPatch，只保留 Source interruption、Handoff 与 Continuation 身份链。
- `src/controller/production/types.ts:ProductionOrchestratorOptions/ProductionContinuationReceipt` — 将 ChangeGuard 降为兼容注入，并让旧 owned-diff 回执字段只读可选；新回执不再生成该字段。
- `src/controller/production/file-production-runtime.ts:executeProductionPlanFile` — Production composition 不再构造 `GitChangeGuard`。
- `src/controller/continuation/continuation-coordinator.ts:createEnvelope/performWorkflow/decodeProgressReceipt` — 新请求省略 owned-diff binding；旧字段仅条件式重建幂等材料，不再作为 ProgressReceipt 门禁。
- `src/controller/continuation/types.ts:ResumeEnvelope/ContinueFromHandoffInput` — 把旧 digest 字段收敛为可选兼容输入。
- `test/production-orchestrator.test.ts`、`test/continuation.test.ts` — 锁定 timeout 四类 workspace inspection 零调用、三 UUID 隔离、新字段省略与旧字段无门禁重放。

**入口**:`ProductionOrchestrator.recoverTimedOutCompaction` 在持久 `SOURCE_INTERRUPTING` 状态启动三任务接力；旧调用可继续进入 `ContinuationCoordinator.continueFromHandoff`。
**测试**:`node --test dist/test/production-orchestrator.test.js dist/test/continuation.test.js dist/test/handoff.test.js dist/test/file-production-runtime.test.js` 为 36/36；revision 漂移与 Handoff Evidence 篡改继续失败闭合。

## 2026-08-10 S-PROBE-1 事件优先与分段正文探测

**触达**:
- `src/controller/compaction-monitor/probe-schedule.ts:nextCompactionProbeElapsedMs` — 固定 20/30/35/40 分钟及此后每 2 分钟的下一探测边界。
- `src/controller/production/types.ts:CompactionContentProbePort/CompactionProbeResult` — 把正文读取能力隔离在端口后，只允许三类枚举投影和白名单失败码。
- `src/controller/production/app-server-client.ts:CodexAppServerClient.hostCapabilities` — 默认声明当前结构化事件契约，允许 Host adapter 显式报告 `UNAVAILABLE`。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask.startCompactionProbe/runCompactionProbe` — 调度单次 in-flight probe，丢弃迟到结果，并把压缩投影进既有事件流。
- `src/controller/production/codex-app-server-development-task.ts:completeProbeCompaction` — Turn 终态前闭合已探测压缩，结构化事件到达时优先切回 Host 信号。
- `src/controller/control-plane/recovery-catalog.ts:RECOVERY_CATALOG` — 将 `compaction_probe_failed` 收敛为显式 abort-only 恢复项。

**入口**:`CodexAppServerDevelopmentTask.start(request)`；只有 `host_capabilities.context_compaction_events=UNAVAILABLE` 才调用注入的 `CompactionContentProbePort`。
**测试**:`test/app-server-client.test.ts`、`test/app-server-development-task.test.ts`、`test/compaction-monitor.test.ts` 与 `test/production-orchestrator.test.ts` 覆盖 19:59/20/29:59/30/35/40/42 分钟、事件零 probe、终态取消、单次 in-flight、大小正文等价、canary 隔离与 30 秒接力；全量 184/184。

## 2026-08-10 S-CONTROL-1 监督技能低干扰监控

**触达**:
- `skills/auto-slice-control/SKILL.md:Status-only monitoring` — 把“继续监控”收敛为重复的只读 `status` 命令，限制状态报告字段，并禁止 Worker Task/Thread、Git、工作区、checkpoint 与日志诊断。
- `test/auto-slice-control-skill.test.ts:continue monitoring is a status-only control-plane operation` — 从发布 Skill 读取监控段，锁定唯一控制命令、只读 envelope 与零隐式变更命令。
- `test/auto-slice-control-skill.test.ts:monitoring forbids Worker Task, Git, and workspace diagnostics` — 锁定内容盲监督禁令与有限状态投影字段。
- `test/auto-slice-control-skill.test.ts:NEEDS_USER remains an explicit user-selected recovery` — 锁定显式恢复、旧乐观版本 envelope 与 digest-bound evidence 兼容。

**入口**:用户在启用 `auto-slice-control` Skill 后说“继续监控”或 “continue monitoring”。
**测试**:`node --test dist/test/auto-slice-control-skill.test.js` 为 3/3；`npm test` 为 187/187，并通过 typecheck、lint、官方插件校验与 Markdown links。

## 2026-08-10 S-ACC-1 端到端验收与文档收口

**触达**:
- `scripts/run-s16-content-budget.mjs:productionPlan/captureProductionScenario/main` — 把内容预算门禁扩为八场景最终矩阵，证明两刀可信完成、失败闭合、V1 惰性字段、事件优先、probe 降级、三任务接力及零 Controller Git 调用。
- `test/fixtures/process/fake-s16-app-server.mjs:createSession/afterTurnStarted` — 支持顺序双任务、`FAILED`/`INTERRUPTED`、无 checkpoint 的额外文件与无结构化事件 probe 场景，同时继续注入八类 Worker Content canary。
- `test/fixtures/process/run-s16-production-with-fake-host.mjs:ImmediateProbeScheduler/main` — 显式注入 Host capability、隔离 probe 与虚拟 20 分钟 deadline，并输出有界 probe/协议计数。
- `scripts/verify-s16.mjs:validateSliceSpec/parseEvidence/buildEvidenceArtifacts` — 将 RunStore、Control 与监督 Skill 兼容测试纳入目标门禁，发布 acceptance matrix 并复核八场景可重复性。
- `contracts/slices/S16.json` — 绑定最终可信完成文档输入、兼容测试集与发布候选 unlock。
- `docs/auto-slice-design.md`、`docs/架构.md` — 把产品状态机、Commit Mode、probe 与验收数据流对齐当前实现。

**入口**:`node scripts/run-s16-content-budget.mjs` 运行八场景 harness；`npm run verify:s16 -- --regenerate` 生成最终证据与回执。
**测试**:目标测试集覆盖 Production/App Server/RunStore/Control/Skill；双 harness 锁定 canary-free stdout/stderr、零 full-turn read、零 Controller Git、两刀 `DONE`、两类 `NEEDS_USER` 与事件/probe 分流。

## 2026-08-11 S17 App Server 0.146.0 最小 schema 投影

**触达**:
- `src/controller/production/app-server-protocol-v2.ts:decodeAppServerTurnStartParams` — 投影 text/skill 输入与 camelCase SandboxPolicy，显式要求 `text_elements` 和 workspaceWrite 全五字段。
- `src/controller/production/app-server-protocol-v2.ts:decodeAppServerFreshThreadStartResponse/resolveAppServerSkill` — 锁定 fresh root `turns=[]` 与唯一 enabled skill 解析。
- `src/controller/production/app-server-protocol-v2.ts:decodeAppServerCompletedCommandEvidence/decodeAppServerTurnCompletedNotification` — 对 command/agent/tool item、terminal Turn 和未知 discriminant fail closed。
- `test/fixtures/s17/app-server-0.146.0-schema-projection.json` — 只冻结 S17 所需生成字段、枚举、JSON 默认值与 TypeScript 必填差异。
- `scripts/verify-s17.mjs:generateProjection/verifyFixture` — 无 shell 定位 Codex CLI，重生成 0.146.0 TS/JSON schema、核对最小 projection 并记录 corpus digest。
- `contracts/slices/S17.json` — 冻结 S17 做/不做、负向矩阵、release checks、owned paths 与 S18/S19 解锁条件。

**入口**:`npm run verify:s17`；后续 S18–S21 可直接调用 `production/app-server-protocol-v2` 的 typed codec，不改变当前 Development Task 生命周期。
**测试**:`test/app-server-protocol-v2.test.ts` 覆盖 sandbox 大小写、缺字段、skill 唯一性、fresh turns、completed command、selected tool items 与未知 required variant；release verifier 额外要求本机 `codex-cli 0.146.0` 投影、build/typecheck、全量 test、lint、插件、Markdown 和 diff check 全绿。

## 2026-08-11 S18 Export-owned revision 迁移

**触达**:
- `src/controller/state/types.ts:RunCompactionState` 与 `src/controller/compaction-monitor/compaction-monitor.ts:CompactionMonitor.onStarted` — 新 compaction 写入 S18 marker，旧 marker 缺失状态仍可 replay。
- `src/controller/thread-control/source-interruption-coordinator.ts:decodeInterruptReceipt/decodeThreadInspection/interruptSource` — 验证同 thread/turn interrupted 终态与 revisionless 持久读取，旧在途状态冻结后进入 migration-required。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask.interrupt/inspect` — 绑定原始 terminal signal，发送 `thread/read(includeTurns:false)` 并接受空 `turns`、拒绝 non-empty turns/items 与 delete。
- `src/controller/handoff/compression-handoff-coordinator.ts:createRequest/decodeAndVerifyHandoffReceipt` — 删除请求 revision，改由 export 产出的 canonical `sha256` revision 与 Evidence Index 绑定。
- `scripts/verify-s18.mjs:verifyProductionSurface/runChecks` — 扫描 `src/` 零 legacy revision symbol，并执行 S18 目标矩阵与全量发布门禁。

**入口**:`CompactionMonitor` 超时推进到 `SourceInterruptionCoordinator.interruptSource`，成功后由 `CompressionHandoffCoordinator.exportHandoff` 接续；发布入口为 `npm run verify:s18`。
**测试**:`test/thread-control.test.ts`、`test/app-server-development-task.test.ts`、`test/handoff.test.ts`、`test/run-store.test.ts` 与 `test/production-orchestrator.test.ts` 覆盖 marker/migration、terminal identity、summary-only read、revisionless request 与编排透传。

## 2026-08-11 S19 Fresh-task App Server session 基座

**触达**:
- `src/controller/production/app-server-client.ts:CodexAppServerClient.attachPrivateNotificationRouter/consumeLine` — 在唯一 raw notification 入口先路由私有 fresh-task 事件，未认领帧再进入既有 Controller firewall。
- `src/controller/production/app-server-fresh-task-session.ts:CodexAppServerFreshTaskSessions.start/startTurn` — 发送 exact fresh `thread/start`，证明 root 身份，并执行单 active Turn、顺序 terminal 与 bounded completed-item 投影。
- `src/controller/production/app-server-fresh-task-session.ts:FreshTaskPrivateEventRouter.route` — 按注册 thread/turn 隔离 allowlisted private item，拒绝交叉、迟到、重复与超预算事件且不把正文写进错误。
- `src/controller/production/codex-app-server-development-task.ts:CodexAppServerDevelopmentTask.constructor/dispose` — 支持借用 Host client，并把 client dispose ownership 留给创建者。
- `src/controller/production/codex-app-server-task-host.ts:CodexAppServerTaskHost.constructor/dispose` — 单次创建共享 client，同时注入 Development Task 与 fresh session port，并以唯一 Promise 关闭一次。
- `test/fixtures/process/fake-s19-app-server.mjs` — 提供三 root exact wire、顺序 Turn 和 fresh/identity/content 失败场景。
- `scripts/verify-s19.mjs:verifyProductionSurface/runChecks` — 绑定 S17/S18 receipts，复核 shared client、private router 预算与九项发布门禁。

**入口**:`CodexAppServerTaskHost.fresh_task_sessions.start({kind,source_thread_id,cwd})` 创建 fresh root，再由 session `startTurn(request)` 启动并等待私有 Turn；S20/S21 将在此端口上实现真实 launcher。
**测试**:`test/app-server-fresh-task-session.test.ts` 覆盖三 UUID、一次 initialize、顺序双 Turn、并发拒绝、七类 fresh-root 失败、四类 router 失败、private canary 隔离与 dispose-once；既有 client/development/file-runtime 回归保持全绿。

## 2026-08-11 S20 真实 Compression launcher 与可信 receipt

**触达**:
- `src/controller/handoff/app-server-compression-task-launcher.ts:AppServerCompressionTaskLauncher.start/awaitHandoff` — 唯一解析 export skill，先写 effect/attempt journal，再创建新 artifact 目录并启动 fresh Compression Turn；完成时只接受绑定的 prepare→publish command 链、发布双文件和 Host verifier。
- `src/controller/handoff/app-server-compression-task-launcher.ts:planArtifactAttempt/materializeArtifactAttempt/writeJournal` — 持久递增 attempt，拒绝占用、link/reparse、工作区重叠与目标替换；成功 effect 跨进程直接重放，失败保留 helper workDir。
- `src/controller/handoff/types.ts:HandoffReceiptV2` 与 `src/controller/handoff/compression-handoff-coordinator.ts:decodeAndVerifyHandoffReceipt` — 引入 schema/workflow v2、Compression turn、structural/verify digest，并保留 legacy receipt 给 S21。
- `src/controller/production/codex-app-server-task-host.ts:CodexAppServerTaskHost.constructor` — 默认注入真实 Compression launcher，Continuation 仍 fail closed。
- `test/handoff.test.ts:s20Request/s20Host`、`test/fixtures/process/fake-s20-app-server.mjs`、`test/fixtures/export-codex-handoff/scripts/export-handoff.mjs` — 运行真实子进程 helper，覆盖成功、重放/重试、command/JSON/path/file/digest/consumer/verifier 失败矩阵。
- `scripts/verify-s20.mjs:verifyProductionSurface/runChecks` — 绑定 S18/S19 receipts，复核 journal 顺序、零 final-message 字段与九项发布门禁。

**入口**:`CompressionHandoffCoordinator.exportHandoff` 调用默认 Host 的 `compression_launcher.start/awaitHandoff`；发布入口为 `npm run verify:s20`。
**测试**:`node --test dist/test/handoff.test.js dist/test/file-production-runtime.test.js dist/test/production-orchestrator.test.js` 锁定真实 launcher 与 composition 回归；release verifier 另跑全量 test、build/typecheck、lint、插件、Markdown 和 diff check。

## 2026-08-11 S21 真实 Continuation launcher

**触达**:
- `src/controller/continuation/app-server-continuation-launcher.ts:AppServerContinuationTaskLauncher.start/awaitReady/grantWrite/awaitProgress` — 先复核 Handoff 字节，再以同一 fresh root 的 readOnly/workspaceWrite 双 Turn 从私有终态投影 Ready、Lease 与 Progress 回执。
- `src/controller/continuation/app-server-continuation-launcher.ts:validHandoffReceiptBinding/completeReadTurn/completeWriteTurn` — 绑定 V2 Compression turn 并保留 legacy replay；拒绝工具先于 draft、非 terminal、错误 epoch 与失败 write Turn。
- `src/controller/continuation/continuation-coordinator.ts:decodeAndVerifyHandoffReceipt/createEnvelope/launcherFailureReason` — 同时验证 HandoffReceiptV2 与 legacy schema，把机器 launcher 错误映射到既有失败原因并在 grant 后失败时冻结 epoch。
- `src/controller/production/codex-app-server-task-host.ts:CodexAppServerTaskHost.constructor` — 默认注入真实 Continuation launcher，与 Compression launcher 共用 Host-owned client/fresh session 基座。
- `test/continuation.test.ts:s21Host/s21Envelope` 与 `test/fixtures/process/fake-s21-app-server.mjs` — 覆盖 V2 全链、legacy 重放、Handoff 篡改、Ready 证据、模型伪回执、epoch 与 Progress 失败矩阵。
- `scripts/verify-s21.mjs:verifyProductionSurface/runChecks` — 绑定 S20 receipt，复核双 Turn 顺序、错误映射、零模型字段与九项发布门禁。

**入口**:`ContinuationCoordinator.continueFromHandoff` 调用默认 Host 的 `continuation_launcher.start/awaitReady/grantWrite/awaitProgress`；发布入口为 `npm run verify:s21`。
**测试**:`node --test dist/test/continuation.test.js dist/test/file-production-runtime.test.js dist/test/production-orchestrator.test.js` 锁定真实 launcher、Coordinator 与 composition 回归；release verifier 另跑全量 test、build/typecheck、lint、插件、Markdown 和 diff check。
