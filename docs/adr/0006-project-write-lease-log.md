# Project Write Lease 事件日志

**决策**：active 硬链接独占工作区，不可变事件递进租约状态。

**否决**：
- 可变锁 JSON：并发 epoch 更新和崩溃半写无法同时闭合。
- PID/超时抢锁：无法证明原持有者已停止写入。

**命门**：locator 与 active 必须位于支持原子硬链接的同一文件系统。
**何时回头**：跨主机控制或目标文件系统不支持可靠硬链接。
