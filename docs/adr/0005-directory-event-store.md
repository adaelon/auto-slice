# 目录式事件存储

**决策**：每版状态写为不可变事件文件，快照仅作读取缓存。

**否决**：
- `node:sqlite`：最低 Node 版本仍会暴露实验 API 风险。
- 可变 JSON/JSONL：并发 CAS 与断电半写难以同时闭合。

**命门**：事件与快照目录必须位于支持原子链接和重命名的同一文件系统。
**何时回头**：事件文件规模影响恢复时间，或 SQLite 在最低 Node 版本稳定可用。
