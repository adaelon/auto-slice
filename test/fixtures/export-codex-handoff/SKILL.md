---
name: export-codex-handoff
description: Hermetic S20 process fixture for the production Compression launcher.
---

# Export Codex Handoff fixture

Use `scripts/export-handoff.mjs` with the same `prepare`, `publish`, and
`verify-evidence` argv contract as the installed skill. This fixture exists only
to exercise process, path, digest, and receipt boundaries; it does not summarize
a real Codex rollout.
